#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    // Imprime un saludo en la salida estándar.
    printf("¡Hola Mundo!\n");
    // Termina el proceso.
    exit(EXIT_SUCCESS);
}
